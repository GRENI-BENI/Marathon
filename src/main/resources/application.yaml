spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/bd_kurs
    password: admin
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  main:
    web-application-type: none
app:
  appTitle: Marathon

